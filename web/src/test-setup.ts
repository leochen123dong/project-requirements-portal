/**
 * Vitest setup. Phase 0 stub — Phase 3 tester will extend with
 * @testing-library/jest-dom matchers when @testing-library/react is installed.
 */

// Reserved for Phase 3.
// import { afterEach } from 'vitest';
// import { cleanup } from '@testing-library/react';
// afterEach(() => { cleanup(); });